import { Wallet2 } from 'iconsax-react';
import { IconSaxTypes } from './types';

const WalletIconSax = ({ size = '16', color = '#FFF' }: IconSaxTypes) => (
  <Wallet2 size={size} color={color} />
)

export default WalletIconSax