import { User } from 'iconsax-react';
import { IconSaxTypes } from './types';

const UserIconSax = ({ size = '16', color = '#FFF' }: IconSaxTypes) => (
  <User size={size} color={color} />
)

export default UserIconSax